name: 'PHP CS check'
description: 'PHP Code sniffer check with custom ruleset'
inputs:
  phpcs_paths:
    description: 'Paths to check'
    required: false
    default: './modules'
  enable_warnings:
    description: 'Enable checking for warnings (-w)'
    required: false
    default: 'true'
  phpcs_bin_path:
    description: 'Path to phpcs binary'
    required: false
    default: 'phpcs'
  phpcs_ignore_paths:
    description: 'Paths to ignore'
    required: false
    default: '*/tests/*,*/data/*,*/libraries/*'
  phpcs_extensions:
    description: 'Comma-separated file extensions'
    required: false
    default: ''
  ignore_warnings_on_exit:
    description: 'Exit with 0 when only warnings are found (no errors)'
    required: false
    default: 'false'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.phpcs_paths }}
